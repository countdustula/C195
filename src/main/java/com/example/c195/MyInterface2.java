package com.example.c195;
@FunctionalInterface
/**This is the interface with the percentage calculator function. */
public interface MyInterface2 {
    /**Used in the percentage calculator screen as the other lambda expression. */
    public String percentageCalculator(Double x, Double y);
}
